"""Single source of truth for the client's own version number.

Bump this on every user-visible change -- it shows up in the window title,
the tray tooltip, and the Impostazioni tab, so "which version am I running"
is always a glance away instead of something to dig for in git log.
"""
APP_NAME = "NASBox"
APP_VERSION = "1.10.25"
